(()=>{const q=(s,r=document)=>r.querySelector(s),qa=(s,r=document)=>[...r.querySelectorAll(s)];let opened=false,journey=true,raf;
function enhance(){
 document.body.insertAdjacentHTML('beforeend','<div id="journeyHud"><i></i><span>Journey · playing</span></div><div id="magicLayer" aria-hidden="true"></div>');
 const hud=q('#journeyHud'),magic=q('#magicLayer'); hud.onclick=()=>{journey=!journey;hud.querySelector('span').textContent=journey?'Journey · playing':'Journey · paused'};
 const chapters=qa('.chapter'); const words=['The beginning','You & me','A thousand little moments','Home is you','Forever starts here'];
 chapters.forEach((c,i)=>{if(!q('.cinema-light',c))c.insertAdjacentHTML('beforeend','<div class="cinema-light"></div>');if(!q('.chapter-whisper',c))c.insertAdjacentHTML('beforeend',`<div class="chapter-whisper">${words[i]||'Always'}</div>`);if(i===0)c.classList.add('curtain-scene');if(i===3&&!q('.ring-moment',c))c.insertAdjacentHTML('beforeend','<div class="ring-moment"></div>')});
 function burst(x,y){for(let i=0;i<9;i++){const s=document.createElement('b');s.className='magic-spark';s.style.left=x+'px';s.style.top=y+'px';s.style.setProperty('--x',(Math.random()*100-50)+'px');s.style.setProperty('--y',(Math.random()*-90-20)+'px');s.style.animationDelay=(Math.random()*.12)+'s';magic.appendChild(s);setTimeout(()=>s.remove(),1800)}}
 addEventListener('pointerdown',e=>{if(opened&&!e.target.closest('#journeyHud'))burst(e.clientX,e.clientY)},{passive:true});
 const revealObs=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('cinema-seen');const r=e.target.getBoundingClientRect();if(Math.random()>.35)burst(innerWidth*.5,Math.min(innerHeight*.75,Math.max(100,r.top+innerHeight*.35)))}}),{threshold:.48}); chapters.forEach(c=>revealObs.observe(c));
 const tl=q('.timelinegrid');if(tl){const obs=new IntersectionObserver(es=>es.forEach(e=>{if(!e.isIntersecting)return;const r=e.target.getBoundingClientRect(),p=Math.max(0,Math.min(1,(innerHeight-r.top)/(innerHeight+r.height)));e.target.style.setProperty('--timeline',p*100+'%');[...e.target.children].forEach((k,i,a)=>k.classList.toggle('timeline-lit',p>(i+1)/(a.length+1)))}),{threshold:[0,.1,.25,.5,.75,1]});obs.observe(tl)}
 const seal=q('.seal');if(seal)seal.addEventListener('click',()=>{opened=true;document.body.classList.add('opened');journey=true;burst(innerWidth/2,innerHeight*.58);setTimeout(()=>requestAnimationFrame(autoJourney),2800)},{once:true});
 ['touchstart','wheel','pointerdown'].forEach(ev=>addEventListener(ev,e=>{if(!opened||e.target.closest?.('#journeyHud'))return;journey=false;const h=q('#journeyHud span');if(h)h.textContent='Journey · paused'},{passive:true}));
 function autoJourney(){if(opened&&journey){const el=document.elementFromPoint(innerWidth/2,innerHeight*.62);let speed=1.05;if(el?.closest('.chapter'))speed=1.18;if(el?.closest('.cathedral-cinema'))speed=.78;if(el?.closest('.venues'))speed=.86;if(el?.closest('.actions'))speed=.68;scrollBy(0,speed)}raf=requestAnimationFrame(autoJourney)}
}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',enhance);else enhance();})();
(()=>{const q=(s,r=document)=>r.querySelector(s),qa=(s,r=document)=>[...r.querySelectorAll(s)];let opened=false,journey=true,userPause=false,lastY=0,raf;
function enhance(){document.body.insertAdjacentHTML('beforeend','<div id="journeyHud"><i></i><span>Journey · playing</span></div>');const hud=q('#journeyHud');hud.onclick=()=>{journey=!journey;userPause=!journey;hud.querySelector('span').textContent=journey?'Journey · playing':'Journey · paused'};
const chapters=qa('.chapter');chapters.forEach((c,i)=>{if(!q('.cinema-light',c))c.insertAdjacentHTML('beforeend','<div class="cinema-light"></div>');if(i===0)c.classList.add('curtain-scene');if(i===3&&!q('.ring-moment',c))c.insertAdjacentHTML('beforeend','<div class="ring-moment"></div>')});
const tl=q('.timelinegrid');if(tl){const obs=new IntersectionObserver(es=>es.forEach(e=>{if(!e.isIntersecting)return;const r=e.target.getBoundingClientRect(),p=Math.max(0,Math.min(1,(innerHeight-r.top)/(innerHeight+r.height)));e.target.style.setProperty('--timeline',p*100+'%');const kids=[...e.target.children];kids.forEach((k,i)=>k.classList.toggle('timeline-lit',p>(i+1)/(kids.length+1)))}),{threshold:[0,.1,.25,.5,.75,1]});obs.observe(tl)}
const seal=q('.seal');if(seal)seal.addEventListener('click',()=>{opened=true;document.body.classList.add('opened');journey=true;setTimeout(()=>requestAnimationFrame(autoJourney),3300)},{once:true});
['touchstart','wheel','pointerdown'].forEach(ev=>addEventListener(ev,e=>{if(!opened)return;if(e.target.closest&&e.target.closest('#journeyHud'))return;journey=false;const h=q('#journeyHud span');if(h)h.textContent='Journey · paused'},{passive:true}));
function autoJourney(){if(!opened){raf=requestAnimationFrame(autoJourney);return}if(journey){const y=scrollY;const el=document.elementFromPoint(innerWidth/2,innerHeight*.62);let speed=.62;if(el&&el.closest('.chapter'))speed=.72;if(el&&el.closest('.cathedral-cinema'))speed=.48;if(el&&el.closest('.venues'))speed=.5;if(el&&el.closest('.actions'))speed=.38;scrollBy(0,speed);lastY=y}raf=requestAnimationFrame(autoJourney)}
}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',enhance);else enhance();})();